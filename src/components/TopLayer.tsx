import * as React from 'react'
import withLinks from '../decorators/withLinks'

class TopLayer extends React.Component {
  render() {
    return (
      <div>-70D0 H34D3R-</div>
    )
  }
}

export default withLinks(TopLayer)